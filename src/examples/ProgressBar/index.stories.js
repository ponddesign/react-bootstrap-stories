import { ProgressBar } from "react-bootstrap";

import Animated from "./Animated";
import Basic from "./Basic";
import Contextual from "./Contextual";
import ScreenreaderLabel from "./ScreenreaderLabel";
import Stacked from "./Stacked";
import Striped from "./Striped";
import WithLabel from "./WithLabel";

export default {
  title: "Components/ProgressBar",
  component: ProgressBar,
};

export const animated = Animated;
export const basic = Basic;
export const contextual = Contextual;
export const screenreaderLabel = ScreenreaderLabel;
export const stacked = Stacked;
export const striped = Striped;
export const withLabel = WithLabel;
